{
   "name": "saby-types",
   "version": "25.2000.0",
   "repository": {
      "type": "git",
      "url": "git@git.sbis.ru:saby/Types.git"
   },
   "license": "MIT",
   "files": [
      "index.ts",
      "Types/*"
   ],
   "scripts": {
      "build": "npm run build:install && npm run build:typescript && npm run build:app --inlineSourceMaps",
      "build:install": "wasaby-cli loadProject",
      "build:typescript": "wasaby-cli initTSEnv",
      "build:app": "wasaby-cli buildProject --watcher --inlineSourceMaps",
      "build:autodoc": "wasaby-cli buildAutoDoc --modules=Types",
      "start": "wasaby-cli runUnitTests --server",
      "test": "wasaby-cli runUnitTests --NodeJS --report=console",
      "test:pattern": "wasaby-cli runUnitTests --NodeJS --report=console --grep='ObjectMeta'",
      "test:coverage": "wasaby-cli runUnitTests --NodeJS --coverage",
      "update-cli-store": "wasaby-cli loadProject",
      "lint": "eslint .",
      "lint:fix": "eslint . --fix",
      "lint:css": "stylelint \"**/*.{css,less}\"",
      "lint:css:fix": "stylelint \"**/*.{css,less}\" --fix",
      "prettier": "prettier --write .",
      "prepare": "npm run update-cli-store && npm run build:typescript && wasaby-cli initGitHooks"
   },
   "wasaby-cli": {
      "copy": true,
      "umd": true,
      "preCommitHooks": [
         "Prettier",
         "ESLint",
         "Stylelint"
      ]
   },
   "devDependencies": {
      "wasaby-cli": "git+ssh://git@git.sbis.ru:saby/wasaby-cli.git#rc-25.2000"
   }
}