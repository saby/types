/**
 * Библиотека, которая создает типы из строки.
 * @library Types/parser
 * @includes date Types/_parser/date
 * @public
 */

/*
 * Library that produces types from strings
 * @library Types/parser
 * @includes date Types/_parser/date
 * @public
 * @author Буранов А.Р.
 */

export { default as date } from './_parser/date';
